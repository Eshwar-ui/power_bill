import 'package:flutter/material.dart';

class savepowerpage extends StatelessWidget {
  const savepowerpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
